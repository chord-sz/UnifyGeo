# Checkpoints

Download the released VIGOR checkpoints from the repository root:

```bash
python scripts/download_checkpoints.py --protocol all
```

Checkpoint files are excluded from Git history and verified with SHA-256 after download.
